[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "npm ci",
  "cd ../frontend && npm install"
]

[phases.build]
cmds = ["cd ../frontend && npm run build"]

[start]
cmd = "node server.js"
