{
  "name": "buildmybody",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist/. ../backend/public/ && cd ../backend && npm install",
    "start": "cd backend && node server.js"
  }
}
